const BusinessInfo = () => (
  <div className="min-h-screen flex items-center justify-center bg-background">
    <h1 className="text-2xl font-heading font-bold text-foreground">Etapa 2 — Dados do Negócio</h1>
  </div>
);
export default BusinessInfo;
